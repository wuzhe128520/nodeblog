<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>无畏滴青春账号激活</title>
    <style>
        html {
            min-height: 100%;
            _height:100%;
            height:100%;
        }
        body {
            min-height: 100%;
            _height:100%;
            height:100%;
            background-image: url(/image/bgimg_top.jpg);
        }
        .wrapper {
            position: absolute;
            top: 0;
            bottom: 0;
            left: 0;
            right: 0;
            margin: auto;
            width: 300px;
            height: 200px;
            z-index: 2;
        }
        .tip {
            width: 300px;
            height: 200px;
            display: table-cell;
            vertical-align: middle;
            padding: 10px;
            box-shadow: 1px 1px 50px rgba(0,0,0,.3);
            background: #fff;
            text-align: center;
        }
        .tip span {
            display: inline-block;
        }
        .shadow {
            position: fixed;
            _position: absolute;
            pointer-events: auto;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: #000;
            opacity: 0.3;
            filter: alpha(opacity=30);
            z-index: 1;
        }
    </style>
</head>
<body>
<div class="shadow"></div>
<div class="wrapper">
    <div class="tip">
    <span>
        <%
        var isActive = locals.data.active;
        if(isActive){%>
        <%if(locals.data.username){%>
        <%=locals.data.username%>,
        <%}%>
        <%=locals.data.content%><p id="redirect" style="display: none;text-align: center;"><span id="text">3</span>秒后,跳转到首页</p>

    <script>

        setTimeout(function(){
            var redirect = document.getElementById('redirect'),
                i = 3,
                index = null,
                target = document.getElementById('text');

            redirect.style.display = 'block';
            target.innerHTML = 3;
            index= setInterval(function(){
                target.innerHTML = --i;
                if(i === 1){
                    clearInterval(index);
                    setTimeout(function(){
                        window.location.href = '/login?show=1';
                    },1000);
                }
            },1000);
        },5000);
    </script>
        <%} else { %>
        账号激活失败！<%=locals.data.content%>
        <%}%>
        <%=locals.active%>
    </span>
    </div>
</div>

</body>
</html>